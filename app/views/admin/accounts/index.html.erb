<!-- Page Wrapper -->
<div id="wrapper">
    <%= render "home/sidebar" %>
    
    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

        <!-- Main Content -->
        <div id="content">

            <%= render "home/topbar" %>

            <!-- Begin Page Content -->
            <div class="container-fluid">
                <div class="card border-warning">
                <!-- Page Heading -->
                <div class="card-header bg-dark text-light d-sm-flex align-items-center justify-content-between">
                    <h3>Account GLs</h3>
                    <%= link_to new_admin_account_path, class: "d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm" do %>
                        <i class="fas fa-user-plus fa-sm text-white-50"></i> Create new GL
                    <% end %>
                </div>

                <div class="card-body">
                      <% if notice %>
                        <p style="color: green"><%= notice %></p>
                      <% end %>
                  <table class="table table-striped " id = "user_table">
                  
                    <thead class="bg-def text-light">
                    <div id="example_wrapper" class="row">
                          <div class="col-md-6">
                              <div class="dataTables_filter">
                                  <!-- Add the search input here -->
                              </div>
                              <div class="dataTables_buttons">
                                  <!-- Buttons will be appended here -->
                              </div>
                          </div>
                          <!-- Other content here -->
                      </div>
                      <tr>
                        <th>SN</th>
                        <th>Account name</th>
                        <th>Account GL</th>
                        <th>Options</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% 
                      sn = 0
                      @accounts.each do |account| %>
                      <tr>
                      <td><%= sn += 1 %></td>
                        <td><%= account.name %></td>
                        <td><%= account.gl %></td>
                        <td><%= link_to "view", admin_account_path(account) %></td>
                        
                      </tr>
                      <% end %>
                    </tbody>
                  </table>                 
                  
                </div>
              
              </div>                    
                    
                   
               

            </div>
            <!-- /.container-fluid -->

        </div>
        <!-- End of Main Content -->

        <%= render "home/footer" %>
    </div>
    <!-- End of Content Wrapper -->

</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>

<script>
  $(document).ready(function() {
    var table = $('#user_table').DataTable( {
        lengthChange: false,
        buttons: [
            { extend: 'excel', filename: 'Expense flow branches', text: 'Excel' },
            { extend: 'pdf', filename: 'Expense flow branches', text: 'PDF' },
            { extend: 'colvis', text: 'View Columns' }
        ],
        scrollY: '350px'
    } );
    table.buttons().container()
        .appendTo( '.dataTables_buttons' );
  } );
</script>
